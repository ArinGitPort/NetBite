import { createOperationsChapter, type OperationsConcept } from '@/content/operations-content-helpers';

const c = (id: string, title: string, idea: string, mechanism: string, example: string, result: string, misconception: string, details?: Pick<OperationsConcept, 'illustration' | 'keyTerm'>): OperationsConcept => ({
  id, title, idea, mechanism, example, result, misconception, ...details,
});

export const operationsChapters = [
  createOperationsChapter({
    id: 'ops-01', order: 1, title: 'Transport and Application Endpoints', illustration: 'transport-endpoints',
    summary: 'Connect application services to TCP or UDP endpoints and inspect their state.',
    concepts: [
      c('ops-services', 'Applications Use Network Services', 'A network application exchanges data through a service exposed by another process.', 'The application supplies data to a transport service, which identifies both endpoint processes before IP handles host delivery.', 'A browser process needs a web service on 192.0.2.20.', 'The request needs both the server IPv4 address and the service endpoint.', 'An IPv4 address alone identifies the exact application process.'),
      c('ops-client-server', 'Clients Start and Servers Listen', 'Client and server describe roles in one exchange rather than permanent device types.', 'A server process listens for requests while a client process initiates communication toward that listening endpoint.', 'PC1 starts a request toward a service listening on SERVER1.', 'PC1 is the client for this exchange and SERVER1 is the server.', 'Every powerful computer is always a server in every exchange.'),
      c('ops-ports', 'Ports Identify Application Processes', 'Transport port numbers distinguish processes using the same host address.', 'The destination port selects a receiving service; the source port identifies the client-side endpoint for the reply.', 'PC1 sends from port 53000 to SERVER1 TCP port 443.', 'The server host is selected by IP and its HTTPS process by destination port 443.', 'A switch forwards Ethernet frames by reading TCP port 443.'),
      c('ops-socket-pair', 'Endpoint Tuples Separate Conversations', 'Simultaneous conversations need more identity than one port number.', 'Protocol, source address and port, and destination address and port form a flow identity that keeps replies associated with the correct process.', 'Two PCs both contact 192.0.2.20:443 using different source addresses and ports.', 'The distinct endpoint tuples keep both conversations separate.', 'The destination port alone uniquely identifies every conversation.'),
      c('ops-udp', 'UDP Sends Independent Datagrams', 'UDP provides a small datagram transport without TCP connection and recovery state.', 'Each UDP datagram carries source and destination ports, length, checksum, and application data; delivery and ordering are not guaranteed by UDP.', 'A DNS client sends one query using UDP toward port 53.', 'The datagram can be processed without establishing a TCP connection first.', 'UDP retransmits and reorders every missing datagram automatically.'),
      c('ops-tcp-handshake', 'TCP Establishes Connection State', 'TCP synchronizes endpoint state before carrying an established byte stream.', 'SYN, SYN-ACK, and ACK establish initial sequence state at both endpoints before ordinary application bytes are exchanged.', 'Client port 53000 opens a TCP connection to server port 443.', 'Both endpoints reach established state after the three control exchanges.', 'The first SYN already proves application data was delivered.'),
      c('ops-tcp-reliability', 'TCP Tracks Ordered Bytes', 'TCP reliability depends on sequence numbers, acknowledgments, and retransmission decisions.', 'A receiver acknowledges the next expected sequence position; missing acknowledgment evidence can cause the sender to retransmit outstanding data.', 'One modeled TCP segment is omitted before the receiver advances its acknowledgment.', 'The sender retains unacknowledged data and retransmits it in the guided model.', 'IP routers reorder and retransmit TCP bytes for the endpoints.'),
      c('ops-transport-check', 'Choose Transport from Requirements', 'Transport choice follows application requirements rather than a universal speed rule.', 'Compare the need for ordered reliable stream delivery with tolerance for independent datagrams, then verify ports and endpoint state.', 'A file transfer requires ordered reliable delivery while a small discovery query tolerates a retry.', 'TCP fits the file stream; UDP can fit the independent query.', 'UDP is always faster and TCP is always better.'),
    ],
    lab: ['transport-service-desk', 'Build an application exchange', 'Create TCP and UDP endpoint state and recover a missing TCP delivery'],
    recap: ['Verified transport conversations', 'Services, ports, UDP datagrams, and TCP state', 'How hosts receive configuration automatically'],
  }),
  createOperationsChapter({
    id: 'ops-02', order: 2, title: 'DHCP', illustration: 'dhcp-exchange', summary: 'See how DHCP supplies reusable IPv4 settings to clients.',
    concepts: [
      c('dhcp-purpose', 'Hosts Need Automatic Configuration', 'DHCP gives a host the IPv4 settings it needs instead of making a person enter each value.', 'A DHCP server chooses an address from a configured pool. It can also provide the prefix, default gateway, DNS server, and lease time.', 'A new PC joins VLAN 10 with no IPv4 settings.', 'The PC asks DHCP for a complete, usable configuration.', 'A host can safely guess any unused-looking address and gateway.', { illustration: 'dhcp-settings', keyTerm: 'DHCP' }),
      c('dhcp-transport', 'DHCP Uses Client and Server Ports', 'DHCP uses two well-known UDP ports so the client and server know which process should receive each message.', 'The client sends from UDP port 68 to server port 67. Early messages may be broadcasts because the client does not yet have complete IPv4 settings.', 'PC1 sends DHCPDISCOVER from UDP 68 to UDP 67.', 'The DHCP service—not a web service—receives the message.', 'DHCP uses TCP port 443 because settings must be reliable.', { illustration: 'dhcp-ports', keyTerm: 'UDP 67/68' }),
      c('dhcp-discover', 'Discover Searches for a Server', 'DHCPDISCOVER is the client asking, “Is a DHCP server available for my network?”', 'The client sends a local broadcast. A switch floods it inside the same VLAN. A router stops that broadcast unless a DHCP relay is configured.', 'PC1 broadcasts DHCPDISCOVER in VLAN 10.', 'Hosts in VLAN 10 can receive the frame, but another VLAN cannot receive it directly.', 'Routers forward every DHCP broadcast automatically.', { illustration: 'dhcp-discover', keyTerm: 'DHCPDISCOVER' }),
      c('dhcp-offer', 'Offer Proposes Configuration', 'DHCPOFFER is a proposal from a DHCP server. The client has not accepted it yet.', 'The server finds a free address in the correct pool, skips excluded or leased addresses, and sends the proposed address and options to the client.', 'The pool runs from 192.168.10.100 to 192.168.10.110, but 192.168.10.100 is excluded.', 'The first address this server can offer is 192.168.10.101.', 'Receiving an Offer means every server has committed the same address.', { illustration: 'dhcp-offer', keyTerm: 'DHCPOFFER' }),
      c('dhcp-request-ack', 'Request and Acknowledge Commit a Lease', 'DHCPREQUEST selects an offer. DHCPACK confirms that the client may install the offered settings.', 'The client requests one offered address and identifies the selected server. That server checks the request and sends DHCPACK when the lease is valid.', 'PC1 requests 192.168.10.101 from DHCP1 and receives DHCPACK.', 'PC1 can now install the address, prefix, gateway, and DNS settings.', 'The client should install an address from an unacknowledged Offer.', { illustration: 'dhcp-request-ack', keyTerm: 'DHCPREQUEST / DHCPACK' }),
      c('dhcp-leases', 'Pools and Leases Preserve State', 'A DHCP lease gives an address to one client for a limited period. The server records that assignment in its binding table.', 'A pool contains the addresses the server may offer. Exclusions remove reserved addresses. Existing bindings remove addresses already in use. If none remain, the pool is exhausted.', 'The usable pool contains 192.168.10.101 and 192.168.10.102. Both are already leased when PC3 asks.', 'PC3 receives no offer until an address is released or the pool is expanded.', 'A DHCP server creates unlimited addresses beyond its configured subnet.', { illustration: 'dhcp-pool', keyTerm: 'Lease and binding' }),
      c('dhcp-relay', 'Relays Cross Router Boundaries', 'A DHCP relay helps a client reach a DHCP server on another IP network.', 'The relay receives the client broadcast on the local network, records which client network it came from, and forwards the DHCP message to the server. The server uses that network information to choose the correct pool.', 'PC2 is in 192.168.20.0/24, while DHCP1 is in 192.168.10.0/24.', 'R1 relays the request, so DHCP1 selects the 192.168.20.0/24 pool.', 'Ordinary router broadcast forwarding and DHCP relay are the same behavior.', { illustration: 'dhcp-relay', keyTerm: 'DHCP relay' }),
    ],
    lab: ['dhcp-lease-desk', 'Operate a DHCP service', 'Configure a pool, create and renew leases, exhaust it, and verify relay selection'],
    recap: ['A working DHCP allocation service', 'DORA, pools, leases, options, and relay behavior', 'How names are resolved into addresses'],
  }),
  createOperationsChapter({
    id: 'ops-03', order: 3, title: 'DNS', illustration: 'dns-resolution', summary: 'Resolve application names through records, servers, and cache state.',
    concepts: [
      c('dns-name-address', 'Names and Addresses Solve Different Problems', 'Applications commonly use names while IP forwarding still requires addresses.', 'A resolver translates a requested domain name and record type into usable data before the application opens its transport exchange.', 'A user enters training.example while routing requires an IPv4 destination.', 'DNS must return an applicable address record before IP forwarding can target the server.', 'Routers forward packets by comparing domain-name suffixes.'),
      c('dns-stub', 'The Host Uses a Stub Resolver', 'An application normally asks the local operating-system resolver instead of traversing the DNS hierarchy itself.', 'The stub checks local information and cache, then sends a query to its configured recursive resolver when no current answer exists.', 'PC1 requests the A record for training.example with an empty cache.', 'PC1 queries its configured resolver rather than broadcasting the name on Ethernet.', 'Every DNS query is an Ethernet broadcast to all hosts.'),
      c('dns-recursive', 'A Recursive Resolver Pursues the Answer', 'A recursive resolver accepts responsibility for returning an answer or failure to its client.', 'It uses cached delegations or follows referrals to other DNS servers, retaining useful responses according to their lifetime.', 'Resolver-R receives a query it cannot answer from cache.', 'Resolver-R follows the hierarchy and returns the resulting record to PC1.', 'The client must contact every DNS server itself.'),
      c('dns-hierarchy', 'Authority Is Delegated through the Hierarchy', 'DNS distributes responsibility rather than storing every name on one server.', 'Root and top-level servers provide referrals while an authoritative server supplies records for its configured zone.', 'Resolver-R asks for host.training.example and follows referrals toward the training.example authority.', 'The authoritative server provides the final zone answer.', 'A root server stores the current address of every individual host.'),
      c('dns-records', 'Record Types Answer Different Questions', 'A DNS name can have several record types with distinct purposes.', 'A and AAAA return IPv4 and IPv6 addresses, CNAME supplies an alias, and NS identifies authoritative name servers for a zone.', 'training.example has an A record of 192.0.2.20 and an AAAA record of 2001:db8::20.', 'The requested record type determines which address family is returned.', 'Every DNS record is interchangeable with an IPv4 address.'),
      c('dns-cache', 'TTL Bounds Cached Answers', 'Caching reduces repeated work but cannot retain an answer indefinitely.', 'A resolver stores an answer with its TTL and serves it while logically current; after the learner advances beyond expiry, resolution must refresh it.', 'The A record changes after the modeled cached lifetime expires.', 'The next query follows resolution again and receives the current authoritative value.', 'A cached DNS answer is permanent until the device restarts.'),
      c('dns-troubleshoot', 'Separate Name Failure from Path Failure', 'A failed name lookup and a failed connection are different observations.', 'Check configured resolver reachability, query response, record type, cache state, and then application transport without claiming one failure proves another.', 'Ping by address succeeds, but querying training.example returns no applicable record.', 'IP reachability exists; name resolution is the first demonstrated failure.', 'A DNS failure proves the Ethernet cable is disconnected.'),
    ],
    lab: ['dns-resolution-desk', 'Trace a DNS answer', 'Configure records, follow referrals, inspect cache state, and diagnose resolver failures'],
    recap: ['A guided DNS resolver path', 'Resolvers, authority, records, caching, and evidence', 'How policy permits or denies selected traffic'],
  }),
  createOperationsChapter({
    id: 'ops-04', order: 4, title: 'IPv4 Traffic Policy and ACLs', illustration: 'acl-evaluation', summary: 'Evaluate ordered IPv4 traffic rules at an interface boundary.',
    concepts: [
      c('acl-policy', 'An ACL Expresses a Traffic Policy', 'An access control list translates an intended traffic policy into ordered permit and deny entries.', 'The device classifies a packet using the fields supported by the ACL and applies the first matching action at the configured interface boundary.', 'Users may reach the DNS server but not the administration subnet.', 'Rules must identify permitted DNS traffic and deny the protected destination.', 'An ACL automatically understands an administrator sentence without explicit rules.'),
      c('acl-tuple', 'Traffic Has Matchable Fields', 'Extended IPv4 policy can distinguish protocol, source, destination, and transport ports.', 'The evaluator reads the packet fields actually named by the rule; omitted fields are not secretly inferred from application labels.', 'TCP 192.168.10.10:53000 targets 192.168.20.20:443.', 'An extended rule can match TCP, both networks, and destination port 443.', 'A MAC address alone identifies the remote TCP service.'),
      c('acl-types', 'Standard and Extended ACLs See Different Detail', 'Standard IPv4 ACLs primarily match source addresses while extended ACLs can inspect more packet and transport fields.', 'Choose the narrowest supported rule type that expresses the policy without unintentionally affecting unrelated destinations or services.', 'Only PC1 should be blocked from one remote web service.', 'An extended ACL can express both the source and specific destination service.', 'A standard source-only ACL can precisely select one remote TCP port.'),
      c('acl-wildcard', 'Wildcard Bits Select What Must Match', 'Cisco-style IPv4 ACL syntax uses wildcard masks to mark compared and ignored address bits.', 'A zero wildcard bit must match and a one bit is ignored; 192.168.10.0 with 0.0.0.255 selects the /24 range.', 'A rule targets every host in 192.168.10.0/24.', 'Use network 192.168.10.0 with wildcard 0.0.0.255.', 'A wildcard mask has exactly the same bit meaning as a subnet mask.'),
      c('acl-order', 'First Match Wins', 'ACL entry order is part of the policy.', 'The device tests entries from top to bottom and stops at the first match, so a broad rule placed early can hide a later specific rule.', 'A deny-any entry appears before a permit for DNS.', 'DNS is denied because evaluation stops at the earlier broad match.', 'The device evaluates every rule and lets the most specific entry win.'),
      c('acl-implicit-deny', 'No Match Reaches Implicit Deny', 'An IPv4 ACL does not permit unmatched traffic by default.', 'The device checks each explicit entry in order. If none matches, the implicit deny at the end blocks the traffic, so required traffic needs an explicit permit.', 'The ACL only permits DNS, and a new HTTPS flow matches no entry.', 'The HTTPS flow is denied by the implicit final action.', 'Traffic is automatically permitted when no rule mentions it.'),
      c('acl-direction', 'Interface and Direction Define the Checkpoint', 'A correct rule on the wrong interface or direction does not inspect the intended traffic.', 'Inbound evaluates packets as they enter an interface; outbound evaluates after routing selects the egress interface.', 'A policy must stop VLAN 10 traffic as it enters R1.', 'Apply the ACL inbound on the VLAN 10 routed interface.', 'Inbound and outbound are labels with no effect on packet selection.'),
      c('acl-verify', 'Verification Uses Specific Test Flows', 'A policy is incomplete until permitted and denied examples behave as intended.', 'Inspect counters or matched entries and test required services, blocked services, and unrelated traffic before concluding the ACL is correct.', 'DNS succeeds, administration fails, and HTTPS remains permitted as designed.', 'Together, these tests support the intended policy for those specific flows.', 'One successful ping proves every ACL rule is correct.'),
    ],
    lab: ['acl-policy-desk', 'Apply an IPv4 traffic policy', 'Build an ordered ACL, apply direction, and verify permitted and denied flows'],
    recap: ['A verified interface traffic policy', 'Fields, wildcard masks, ordering, direction, and implicit deny', 'How NAT uses policy to select translations'],
  }),
  createOperationsChapter({
    id: 'ops-05', order: 5, title: 'NAT and PAT', illustration: 'nat-translation', summary: 'Translate selected IPv4 flows across an inside and outside boundary.',
    concepts: [
      c('nat-boundary', 'Private and Global Scope Meet at a Boundary', 'Private IPv4 addresses can be reused internally but are not globally unique Internet identities.', 'A NAT router sits on the path between address realms and modifies selected packet fields according to configured mappings.', 'PC1 192.168.10.10 sends toward documentation server 198.51.100.20.', 'The edge router must route the packet and may translate its inside source identity.', 'A private address becomes globally unique merely because a default route exists.'),
      c('nat-inside-outside', 'Interfaces Establish NAT Direction', 'Translation rules depend on which side of the boundary receives and sends the packet.', 'Inside and outside roles identify the direction in which local and global mappings are applied; incorrect roles prevent the intended lookup.', 'G0/0 faces 192.168.10.0/24 and G0/1 faces the external network.', 'G0/0 is the inside boundary and G0/1 is the outside boundary.', 'Inside and outside describe whether an interface is physically indoors.'),
      c('nat-static', 'Static NAT Preserves One Mapping', 'Static NAT maintains a configured one-to-one address mapping.', 'Outbound packets replace the inside local source with its inside global mapping; return packets reverse the destination mapping.', '192.168.10.10 is mapped to 203.0.113.10.', 'The same configured global identity maps back to PC1.', 'Static NAT automatically assigns a different public address for every flow.'),
      c('nat-pool', 'Dynamic NAT Allocates from a Pool', 'Dynamic NAT selects an available global address for a matching inside source.', 'The traffic-selection rule identifies eligible sources and the pool supplies mappings until its available addresses are exhausted.', 'Two pool addresses are already mapped when a third eligible host begins traffic.', 'The third host cannot obtain a dynamic mapping until capacity is available.', 'Dynamic NAT invents additional global addresses when its pool is full.'),
      c('nat-pat', 'PAT Adds Transport Identity', 'PAT allows several inside flows to share one global IPv4 address by translating transport identifiers.', 'The table maps protocol, inside address and port to a unique global address and port so return traffic can find the correct endpoint.', 'Two PCs contact TCP port 443 using different source tuples through 203.0.113.5.', 'Distinct translated source ports keep the return flows separate.', 'PAT identifies every flow using only the shared global IPv4 address.'),
      c('nat-table', 'The Translation Table Is Forwarding State', 'A translation result depends on current mappings rather than a decorative address change.', 'Outbound eligible traffic creates or uses a mapping; return traffic must match its translated protocol and endpoint tuple.', 'A reply targets 203.0.113.5:40001 and a matching PAT entry exists.', 'The router reverses the mapping toward its recorded inside endpoint.', 'Any unsolicited outside packet matches whichever inside host is online.'),
      c('nat-return', 'Return Traffic Must Cross the Same Translator', 'Stateful translation requires the corresponding response path through the device holding the mapping.', 'Routing must deliver the return tuple to the NAT outside interface, where the table lookup rebuilds the inside destination.', 'The external route sends the reply around a different edge router.', 'The original NAT mapping cannot translate a packet it never receives.', 'NAT state automatically synchronizes across every router.'),
      c('nat-troubleshoot', 'Translation Has a Dependency Chain', 'A missing translation can result from routing, interface roles, traffic selection, pool capacity, or return-path errors.', 'Check source eligibility and direction first, then inspect table creation, route reachability, and the reverse match.', 'The source ACL does not match 192.168.20.10, so no PAT entry appears.', 'Correct the selection rule before changing DNS or switch MAC state.', 'An empty NAT table always proves the outside link is physically down.'),
    ],
    lab: ['nat-translation-desk', 'Translate inside flows', 'Configure static and PAT rules, inspect mappings, and verify the return path'],
    recap: ['A verified NAT and PAT boundary', 'Mappings, pools, ports, state, and return traffic', 'How IPv6 represents addresses without NAT44 assumptions'],
  }),
  createOperationsChapter({
    id: 'ops-06', order: 6, title: 'IPv6 Addressing', illustration: 'ipv6-addressing', summary: 'Read, shorten, classify, and configure IPv6 interface addresses.',
    concepts: [
      c('ipv6-purpose', 'IPv6 Uses a Larger Address Space', 'IPv6 defines 128-bit addresses and a revised IP packet format rather than extending dotted-decimal IPv4.', 'Hosts and routers still compare prefixes and select next hops, but IPv6 uses its own address types and control mechanisms.', 'A new branch is assigned documentation prefix 2001:db8:10::/64.', 'Interfaces need IPv6 addresses and prefix context belonging to that network.', 'IPv6 is an encrypted version of IPv4.'),
      c('ipv6-hex', 'IPv6 Uses Eight Hexadecimal Groups', 'A full IPv6 address contains eight 16-bit hexadecimal groups.', 'Each hexadecimal digit represents four bits, so four digits form one 16-bit group and eight groups represent 128 bits.', '2001:0db8:0010:0000:0000:0000:0000:0010 is shown in full.', 'The address contains eight groups even when later written more compactly.', 'Each colon-separated group is an IPv4-style decimal octet.'),
      c('ipv6-leading-zero', 'Leading Zeros May Be Omitted', 'Leading zero digits inside each IPv6 group are optional display characters.', 'Remove only zeros at the left of a group while retaining at least one digit when that group is not part of double-colon compression.', '0db8 becomes db8 and 0010 becomes 10.', 'The shortened groups preserve the same 16-bit values.', 'Trailing zeros can always be removed from a group without changing its value.'),
      c('ipv6-double-colon', 'One Double Colon Compresses Zero Groups', 'A consecutive run of all-zero groups may be replaced once by double colon.', 'Expand the address to exactly eight groups to verify how many zero groups the double colon represents; using it twice would be ambiguous.', '2001:db8:10:0:0:0:0:10 becomes 2001:db8:10::10.', 'The compact and expanded strings identify the same 128-bit address.', 'Several double colons can independently compress different zero runs.'),
      c('ipv6-prefix', 'IPv6 Prefixes Mark Network Identity', 'IPv6 prefix length counts leading network bits just as classless IPv4 prefixes do.', 'Interfaces compare the prefix-defined leading bits to decide whether a destination is on-link or requires a router.', '2001:db8:10::25/64 sends to 2001:db8:10::40/64.', 'Both addresses share the same first 64 bits and are on-link in this model.', 'The final hexadecimal group alone determines the IPv6 network.'),
      c('ipv6-global', 'Global Unicast Identifies Routed Interfaces', 'Global unicast addresses are intended for unicast routing beyond one local link.', 'The configured prefix and interface identifier form an address whose route scope differs from a link-local identity.', 'R1 uses 2001:db8:10::1/64 on the branch LAN.', 'Hosts can use it as the routed gateway identity for that prefix.', 'Every IPv6 address beginning with 2001 is publicly assigned to NetBite.'),
      c('ipv6-link-local', 'Link-Local Addresses Stay on One Link', 'IPv6 interfaces use FE80::/10 link-local addresses for same-link control and next-hop communication.', 'Routers do not forward link-local packets to another link, and duplicate link-local text can exist on different interfaces when interface scope distinguishes it.', 'PC1 reaches its local router using FE80::1 on the LAN interface.', 'That next-hop identity is meaningful only on the selected local link.', 'A link-local destination can be routed across the Internet.'),
      c('ipv6-multicast-anycast', 'IPv6 Uses Multicast and Anycast Roles', 'IPv6 has no broadcast address and uses multicast for selected groups; anycast uses a unicast address assigned to multiple interfaces.', 'Multicast targets subscribed interfaces while routing delivers an anycast destination toward one suitable instance according to routing state.', 'Neighbor Discovery targets a solicited-node multicast group instead of every host.', 'Only relevant local listeners process the group delivery.', 'IPv6 multicast and IPv4 broadcast are identical global flooding mechanisms.'),
    ],
    lab: ['ipv6-address-desk', 'Configure IPv6 interfaces', 'Expand, compress, classify, and validate addresses and /64 prefixes'],
    recap: ['Validated IPv6 interface identities', 'Hex groups, compression, prefixes, and address scopes', 'How IPv6 discovers neighbors and routers'],
  }),
  createOperationsChapter({
    id: 'ops-07', order: 7, title: 'IPv6 Local Delivery and Routing', illustration: 'ipv6-neighbor-delivery', summary: 'Use ICMPv6 control messages to discover neighbors and reach routed destinations.',
    concepts: [
      c('icmpv6-role', 'ICMPv6 Supports Core IPv6 Operation', 'ICMPv6 carries errors, diagnostics, and control messages needed by IPv6 hosts.', 'IPv6 Neighbor Discovery and router discovery use defined ICMPv6 messages rather than IPv4 ARP frames.', 'PC1 needs the link-layer address of 2001:db8:10::20.', 'PC1 begins an ICMPv6 Neighbor Solicitation process.', 'ICMPv6 is used only when a ping command is typed.'),
      c('ipv6-no-arp', 'IPv6 Neighbor Discovery Replaces ARP', 'IPv6 does not use ARP to map a local next hop to a link-layer address.', 'The sender derives a solicited-node multicast destination and transmits a Neighbor Solicitation containing the target IPv6 address.', 'PC1 resolves local PC2 before sending the unicast frame.', 'Neighbor Discovery supplies the link-layer mapping used for local delivery.', 'PC1 broadcasts an IPv4 ARP Request for the IPv6 target.'),
      c('ipv6-na', 'The Target Advertises Its Mapping', 'An interface owning the target IPv6 address can answer a valid Neighbor Solicitation.', 'A Neighbor Advertisement reports the target link-layer information and lets the requester update its neighbor cache.', 'PC2 owns 2001:db8:10::20 and answers PC1.', 'PC1 records PC2 and its link-layer address in the neighbor cache.', 'Every host receiving the multicast solicitation claims the target.'),
      c('ipv6-router-discovery', 'Hosts Discover Local Routers', 'Router Solicitation and Router Advertisement messages communicate local router and prefix information.', 'A host may solicit immediately; an enabled router advertises its presence and flags or prefixes used by host configuration.', 'R1 advertises 2001:db8:10::/64 and its link-local identity.', 'PC1 learns an on-link prefix and a candidate default router.', 'A switch generates the IPv6 default gateway for every VLAN.'),
      c('ipv6-slaac-dad', 'SLAAC Builds and Checks an Address', 'Stateless address autoconfiguration combines advertised prefix information with an interface identifier and verifies uniqueness.', 'The host first forms a tentative address. It performs Duplicate Address Detection, or DAD, and uses the address only when no other device reports that address as its own.', 'PC1 forms 2001:db8:10::a/64 and no duplicate response appears.', 'The address becomes usable after the DAD check finds no reported duplicate.', 'SLAAC assigns addresses from a DHCPv4 address pool.'),
      c('ipv6-next-hop', 'Prefix Comparison Selects the Next Hop', 'IPv6 hosts still distinguish on-link from routed destinations.', 'The host compares the destination with known on-link prefixes; it resolves the destination neighbor locally or resolves the selected router for a remote prefix.', 'PC1 in 2001:db8:10::/64 sends to 2001:db8:20::20.', 'PC1 resolves its router rather than the remote host.', 'IPv6 hosts use direct delivery for every global address.'),
      c('ipv6-static-route', 'IPv6 Static Routes Connect Remote Prefixes', 'Routers need a connected, static, or learned route for every remote IPv6 prefix they forward toward.', 'A static route identifies a destination prefix and local next hop; Neighbor Discovery then resolves that next hop on the outgoing link.', 'R1 receives a packet for 2001:db8:20::20.', 'R1 matches 2001:db8:20::/64 and resolves its configured next-hop neighbor.', 'A default router on the source host automatically teaches every intermediate router the return path.'),
      c('ipv6-route-trouble', 'IPv6 Verification Needs Both Directions', 'A valid source address and neighbor entry do not alone prove routed return reachability.', 'Check interface state, prefix membership, neighbor resolution, selected route, destination link, and return route in that order.', 'The request reaches Branch-B but R2 lacks a return route to 2001:db8:10::/64.', 'The modeled Echo exchange stops on the return routing decision.', 'A Neighbor Advertisement proves every remote route exists.'),
    ],
    lab: ['ipv6-neighbor-desk', 'Trace IPv6 delivery', 'Resolve neighbors, learn a router, configure a static path, and repair return reachability'],
    recap: ['A working routed IPv6 exchange', 'ICMPv6, NDP, SLAAC, next hops, and return paths', 'How switches prevent redundant links from looping'],
  }),
  createOperationsChapter({
    id: 'ops-08', order: 8, title: 'Redundant Switching and STP', illustration: 'spanning-tree', summary: 'Calculate one loop-free forwarding tree across redundant Layer 2 links.',
    concepts: [
      c('stp-redundancy', 'Redundant Links Provide Alternate Paths', 'A second physical path can preserve connectivity after a failure but creates multiple Layer 2 paths.', 'Switching redundancy requires a control decision that keeps an alternate available without forwarding ordinary traffic through every path simultaneously.', 'Three switches form a triangle with all links active.', 'A spanning-tree process must select a loop-free forwarding subset.', 'Adding more switch links is always safe without a control protocol.'),
      c('stp-loop', 'Layer 2 Loops Repeat Frames', 'Ethernet frames do not contain an IP-style hop limit that stops a switching loop.', 'Broadcast, unknown-unicast, or duplicated frames can circulate, multiply, and cause MAC-table instability when redundant ports all forward.', 'A broadcast enters a three-switch physical loop.', 'The same frame can return through another port and be forwarded again.', 'A router TTL automatically stops an Ethernet-only loop.'),
      c('stp-bpdu', 'BPDUs Advertise Bridge Information', 'Switches exchange bridge protocol information to agree on one loop-free path.', 'A switch compares the advertised root bridge, total path cost, sender bridge ID, and sender port ID in that order. It keeps the best received information.', 'SW1 advertises a lower bridge ID than SW2 and SW3.', 'The other switches accept and pass along SW1 as the better root candidate.', 'Switches elect the root by comparing their management IPv4 addresses.'),
      c('stp-root', 'The Lowest Bridge ID Becomes Root', 'The root bridge provides the reference point for path selection.', 'Bridge priority is compared before the MAC-derived identifier; the switch with the lowest complete bridge ID wins.', 'SW2 has priority 24576 while the others use 32768.', 'SW2 becomes root even if another switch has a lower MAC value.', 'The switch with the greatest number of ports becomes root.'),
      c('stp-root-port', 'Each Nonroot Switch Selects a Root Port', 'A nonroot switch chooses its best path toward the root bridge.', 'The lowest received root path cost wins. If costs match, STP compares the sender bridge and port identifiers in a defined order.', 'SW3 has paths costing 4 and 8 toward the root.', 'Its cost-4 interface becomes the root port.', 'Every interface facing the root is automatically a root port.'),
      c('stp-designated', 'Each Segment Selects a Designated Port', 'One forwarding port represents the best path toward the root on each segment.', 'The switch advertising the superior root information on that segment supplies the designated port; another redundant port can become alternate.', 'SW1 and SW3 share a segment not directly attached to the root.', 'The superior advertisement decides which side forwards for that segment.', 'Both ends of every redundant segment must forward ordinary frames.'),
      c('stp-states', 'Roles Produce Forwarding or Discarding State', 'A port role describes its purpose, while its state determines whether ordinary data frames cross it.', 'Root and designated ports forward ordinary frames. An alternate port discards ordinary frames but can still receive the control information STP needs.', 'All three physical links remain connected after the election.', 'One redundant port discards data, leaving a loop-free active path.', 'A discarding port is physically unplugged and cannot participate in recalculation.'),
      c('stp-change', 'A Failed Path Triggers Recalculation', 'The alternate topology matters when a forwarding path becomes unavailable.', 'Remove the failed link, compare the remaining bridge information again, and assign new root and designated roles without inventing a convergence duration.', 'SW3 loses its current root-port link while an alternate remains.', 'The recalculated tree activates the best remaining loop-free path.', 'STP guarantees zero interruption and a fixed recovery time in every network.'),
    ],
    lab: ['spanning-tree-desk', 'Build a loop-free tree', 'Elect a root, calculate port roles, fail a link, and verify the recalculated path'],
    recap: ['A redundant loop-free switched topology', 'BPDUs, root election, costs, roles, and recalculation', 'How compatible links form one logical bundle'],
  }),
  createOperationsChapter({
    id: 'ops-09', order: 9, title: 'EtherChannel', illustration: 'etherchannel', summary: 'Bundle compatible physical links into one logical LACP connection.',
    concepts: [
      c('ec-purpose', 'A Bundle Acts as One Logical Link', 'EtherChannel combines compatible physical links so other switching logic can treat them as one interface.', 'Member links join a port-channel only after both endpoints agree on required settings and LACP state.', 'Two parallel switch links are intended to carry the same VLAN trunk.', 'A valid port-channel provides one logical forwarding relationship.', 'Parallel links automatically become one safe bundle without configuration.'),
      c('ec-lacp', 'LACP Negotiates Membership', 'LACP exchanges information that identifies compatible aggregation partners and member ports.', 'At least one side must use active mode to initiate. Two passive endpoints wait and do not form a negotiated channel.', 'SW1 is active and SW2 is passive for channel group 1.', 'LACP can form the channel when the other settings match.', 'Two passive LACP endpoints always initiate negotiation.'),
      c('ec-compatibility', 'Members Need Matching Configuration', 'Links in one bundle must present compatible switching properties.', 'The simulator compares administrative state, speed class, access or trunk mode, native context within scope, and allowed VLANs before admitting a member.', 'F0/1 permits VLANs 10 and 20 while F0/2 permits only VLAN 10.', 'The mismatched member is suspended until trunk settings agree.', 'LACP silently rewrites every mismatched VLAN setting.'),
      c('ec-stp', 'STP Sees the Logical Port-Channel', 'A formed EtherChannel should not appear as several independent parallel STP paths.', 'Spanning tree evaluates the port-channel as one logical link while LACP manages the physical members beneath it.', 'Two members form Po1 between SW1 and SW2.', 'STP assigns its role to Po1 rather than separately blocking one member as a loop.', 'STP must block every second member of a valid channel.'),
      c('ec-verify', 'Verify Bundle and Member State', 'A configured channel group is not proof that all intended links joined.', 'Inspect protocol, logical interface state, member flags, and VLAN/trunk consistency, then test reachability across the logical path.', 'Po1 is up but F0/2 is suspended for a configuration mismatch.', 'Traffic can use the formed member, while the mismatch still requires correction.', 'An up port-channel proves every physical member is active.'),
      c('ec-failure', 'A Member Failure Need Not Remove the Bundle', 'A port-channel can remain operational while at least one valid member still provides a path.', 'The failed member leaves active selection. The logical link remains when its minimum requirements are still met.', 'F0/1 fails while compatible F0/2 remains active in Po1.', 'Po1 remains available through F0/2 in this example.', 'One member failure always disables the entire logical channel.'),
    ],
    lab: ['etherchannel-desk', 'Form an LACP bundle', 'Configure both endpoints, correct a suspended member, and verify the logical trunk'],
    recap: ['A verified LACP port-channel', 'Negotiation, compatibility, logical state, and failure handling', 'How routers learn and compare dynamic route sources'],
  }),
  createOperationsChapter({
    id: 'ops-10', order: 10, title: 'Dynamic Routing Foundations', illustration: 'dynamic-route-selection', summary: 'Compare route sources and metrics before learning one routing protocol.',
    concepts: [
      c('dynamic-purpose', 'Routers Can Exchange Reachability', 'Dynamic routing protocols communicate network reachability so administrators do not manually enter every changing path.', 'A protocol builds control-plane state from neighbors and topology information, then offers selected routes to the routing table.', 'R1 learns that 192.168.30.0/24 is reachable through R2.', 'The learned route can replace missing manual knowledge while the protocol remains valid.', 'Dynamic routing sends user application data to discover a route.'),
      c('control-data', 'Control Plane Builds, Data Plane Forwards', 'Route learning and packet forwarding are related but distinct responsibilities.', 'The control plane calculates or receives candidate routes; the forwarding plane uses the installed best route for each destination packet.', 'OSPF state changes before a later packet is sent.', 'The routing table is updated first, then forwarding uses the installed result.', 'Every packet reruns the complete routing-protocol algorithm itself.'),
      c('route-sources', 'Route Codes Identify Sources', 'Connected, static, and dynamically learned routes can all become candidates.', 'A route source records how the router learned the prefix, while prefix length still determines which destination ranges match.', 'R1 has connected, static, and OSPF entries in its table.', 'The displayed source code explains origin, not whether the route is longest for every destination.', 'An OSPF code means the route always defeats a connected route.'),
      c('admin-distance', 'Administrative Distance Compares Sources', 'When different route sources offer the same prefix, a router needs a trust preference.', 'For matching prefixes from different sources, the router prefers the lower administrative distance. Protocol metrics are separate and compare paths learned by the same routing source.', 'A static and OSPF route both advertise 192.168.30.0/24.', 'The configured source preference decides which candidate is installed.', 'Administrative distance is added to OSPF path cost for every comparison.'),
      c('route-metric', 'A Metric Compares Paths within a Protocol', 'A routing protocol can offer several paths to the same prefix.', 'After prefix and source preference, the protocol-specific metric selects its preferred path; metrics from unrelated protocols are not directly comparable.', 'OSPF offers costs 20 and 30 for the same remote /24.', 'The cost-20 OSPF path is preferred among those candidates.', 'The largest metric always represents the fastest path.'),
      c('convergence-boundary', 'Topology Change Requires New Control State', 'A failed path can invalidate neighbors and learned routes.', 'When a known adjacency fails, the routers must update their topology information and calculate routes again. NetBite shows the resulting route but does not invent a real convergence time.', 'The preferred link fails while a higher-cost OSPF path remains.', 'After recalculation, the remaining path becomes the installed route.', 'Every routing protocol converges instantly and identically in production.'),
    ],
    lab: ['route-source-desk', 'Select installed routes', 'Compare prefixes, route sources, administrative distance, metrics, and failures'],
    recap: ['A verified dynamic route selection table', 'Control state, route sources, preference, metrics, and change', 'How OSPF builds single-area link-state routes'],
  }),
  createOperationsChapter({
    id: 'ops-11', order: 11, title: 'Single-Area OSPFv2', illustration: 'ospf-topology', summary: 'Form OSPF neighbors and calculate IPv4 routes from shared link-state information.',
    concepts: [
      c('ospf-link-state', 'OSPF Shares Link-State Information', 'OSPF routers describe relevant links so routers in an area can calculate a consistent topology.', 'Eligible neighbors exchange and synchronize link-state advertisements. Each router then runs its own shortest-path calculation.', 'R1, R2, and R3 participate in area 0.', 'Their link-state databases describe the same active area topology.', 'OSPF forwards every user packet through the link-state database.'),
      c('ospf-router-id', 'Router ID Identifies the OSPF Router', 'OSPF needs a stable 32-bit router identifier separate from ordinary packet destination addressing.', 'The configured or selected router ID distinguishes one OSPF router from another in neighbor and database records.', 'R1 is configured with router ID 1.1.1.1.', 'Its LSAs and neighbor records use that OSPF identity.', 'The router ID must be an address currently assigned to the outgoing interface.'),
      c('ospf-hello', 'Hello Messages Discover Neighbors', 'OSPF-enabled interfaces send Hello messages to discover compatible routers on the link.', 'Before becoming neighbors, the routers check important shared settings such as area, subnet, Hello and Dead timers, authentication, and interface state.', 'R1 and R2 share a /30 but use different OSPF areas.', 'They do not become neighbors until the area settings match.', 'Any two routers that can ping must automatically become OSPF neighbors.'),
      c('ospf-adjacency', 'Adjacency Requires Compatible State', 'Physical link-up is only one prerequisite for an OSPF adjacency.', 'Both interfaces must be active, addressed on the shared network, enabled for OSPF, and compatible for the modeled area and network type.', 'R2 G0/1 is shut while every OSPF command remains configured.', 'The neighbor path stays down because the parent interface is not operational.', 'OSPF configuration overrides an administratively disabled interface.'),
      c('ospf-lsdb', 'LSAs Build the Area Database', 'Neighbor formation permits routers to exchange current topology descriptions.', 'The simulator originates links, floods them through established adjacencies, de-duplicates advertisement identity, and constructs an area graph.', 'R3 advertises connected network 192.168.30.0/24 through area 0.', 'R1 learns the prefix in its synchronized area database.', 'An LSA is the same thing as a user IPv4 packet carrying application data.'),
      c('ospf-spf', 'SPF Selects the Lowest-Cost Path', 'Each router calculates paths from itself across the shared topology graph.', 'The shortest-path-first calculation adds interface costs and records the next hop for each reachable advertised prefix.', 'R1 can reach R3 with total costs 20 or 40.', 'R1 selects the cost-20 OSPF path.', 'OSPF selects the path with the fewest router names regardless of cost.'),
      c('ospf-install', 'OSPF Offers Routes to the Routing Table', 'A calculated OSPF path is still subject to the router’s ordinary route installation rules.', 'The candidate records prefix, next hop, outgoing interface, source, administrative distance, and OSPF cost before route-source selection.', 'R1 calculates an OSPF route to 192.168.30.0/24 while an equally specific static route exists.', 'Route-source preference decides which same-prefix candidate is installed.', 'OSPF cost alone defeats every route from another source.'),
      c('ospf-troubleshoot', 'Troubleshoot the First Broken OSPF Dependency', 'Missing routes can result from interface, addressing, participation, compatibility, database, or installation state.', 'Check interface and subnet first, then neighbor state, LSDB presence, SPF reachability, route-source preference, and return routing.', 'R1 has no R2 neighbor because their area IDs differ.', 'Correct the adjacency mismatch before changing downstream network advertisements.', 'Adding a default route repairs every OSPF neighbor mismatch.'),
    ],
    lab: ['ospf-area-desk', 'Build an OSPF area', 'Configure three routers, form neighbors, inspect topology state, and recover from a link failure'],
    recap: ['A verified single-area OSPF topology', 'Neighbors, LSAs, SPF cost, installed routes, and troubleshooting', 'Complete the Network Operations capstone'],
  }),
];
