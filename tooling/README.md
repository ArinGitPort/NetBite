# Repository tooling

This directory contains automation that operates across applications or packages.

- `content/` contains curriculum import and validation utilities.
- `validation/` contains repository architecture checks.

Application-specific scripts remain inside their owning application.
